# 引き継ぎメモ 2026-09-11 夜 — 第272〜275便（マイページの手直しと、求人の自動配信）

★ 第271便までは「駅ちか・エステ魂へ登録」の話。★ ここから先は【見た目の手直し】と【フクエスワーク】。
★★★ 今日の push は4本: **第272便・第273便（e2f19a8f）・第274便（c5aebb29）・第275便**。

---

## 0. ★★★ いまどうなっているか（5行）

- ★★ マイページの「今すぐ」「セラピスト」「出勤」の見た目を作り直した。★ 並びは**あいうえお順 → 非公開**。
- ★★★ **取り返しのつかない削除を画面から消した**。★ 求人＝完全撤去／セラピスト＝**非公開にしてからの2段階**。
- ★★ 応募の未対応件数を**赤丸**で3か所に出す（ワークのホーム・ワークのサイドバー・マイページの関連サイト）。
- ★★★ 求人の新着情報に**自動配信**（1日1回・順番に1本）。★ SQL適用済み・実弾も通った。★ **cron はまだ**。
- ★★ フクエスワークのTOPに**店舗新着情報**ブロック（1店舗1件）＋一覧 `/jobs/news`（最新50件）。

---

## 1. ★ 触ったもの

```
第272便  src/app/mypage/page.tsx            今すぐカード・並び順・セラピストの2列ほか
         src/lib/therapistOrder.ts          sortTherapistsByKana を新設（あいうえお順→非公開）
第273便  src/app/mypage/page.tsx            セラピスト削除の2段階化・写真右上へ・応募バッジ
         src/app/mypage/jobs/*              削除ボタン撤去・ロゴからホーム・状態バッジ・文言
         src/app/mypage/jobs/useNewApplications.tsx  新規（未対応件数のフック＋赤丸）
         src/app/components/CouponCard.tsx  「ご利用の際は」を削除
第274便  src/app/lib/jobs.ts                WORK_NEWS_MAX 20 → 10
         src/app/actions/jobs.ts            getNewApplicationCount を新設（件数だけ）
         src/app/api/admin/work-news-auto/  新規（自動配信の周）
         supabase/migrations/20260911_work_news_auto.sql  新規（適用済み）
         src/app/mypage/JobNewsManager.tsx  「自動で回す」チェック・「自動配信中」の印・周期の案内
         src/app/jobs/[id]/JobNewsList.tsx  画像を比率固定（スマホ4:3・PC16:9）
第275便  src/app/lib/workNewsFeed.ts        新規（TOPブロックと一覧の共通取得）
         src/app/jobs/WorkNewsFeedList.tsx  新規（行リスト）
         src/app/jobs/page.tsx              マッチングの下にブロック
         src/app/jobs/news/page.tsx         新規（もっと見る＝最新50件）
```

---

## 2. ★★ 決めごと

### 2-1. ★★★ 削除は「消せない」より「消しにくい」へ

- ★ **求人の削除は撤去**（`/mypage/jobs`）。★ 求人と**応募履歴が一緒に消えて戻せない**ため。
  ★ サーバー側の `deleteMyJob` は残してある（★ 運営が使う口）。
- ★★ **セラピストの削除は【非公開にした行にだけ】出す**。★ 手間を2段階に:
  ① 「編集」→ サイトへの掲載で**非公開** → ② 写真の右上に出る「削除」→「**本当に削除**」
  ★ 画面に出さないだけでなく、処理側でも `is_active !== false` を弾いている（★ 二重に止める）。
- ★ 残したもの: 予約の削除（キャンセルがある）／媒体連携のログイン情報（2段階・入れ直せる）／
  写メ日記・新着情報の削除（日常の操作）。★ カッキーさんの選択。

### 2-2. ★ 並び順（出勤ページ・セラピストページ）

- ★ **公開の方をあいうえお順 → そのあと非公開**（中もあいうえお順）。★ `sortTherapistsByKana` の1か所だけ。
- ★ 出勤・写真の有無は**見ない**。★ 出勤を入れるたびに順番が飛んで入力しづらかったため（カッキーさんの申告）。
- ★★ 漢字は読みで並ばない（かなの後ろに固まる）。★ 検索バーと同じ限界。★ 直すなら読みがなの列が要る。
- ★ 写メ日記の並びは**今までどおり**（揃えるかは未決）。

### 2-3. ★★ 応募バッジ —— 一度作って、外して、作り直した

- ★ 最初は `getJobApplications`（**全行**を取る）を60秒ごとに回していた。
  ★ 数を出すだけなのに応募者の氏名・電話を毎回運ぶ＋店舗数ぶん通信が走る。★ カッキーさんの指摘で撤去。
- ★★ 作り直し: **件数だけ返す** `getNewApplicationCount`（`count:'exact', head:true`・クエリ1本）。
  ★ **定期更新はしない**。★ 数え直すのは「ページを開いたとき」と「タブに戻ったとき」だけ。
- ★ 数える対象は `status = 'new'` のみ。★ 連絡済み・クローズ・削除で消える。

### 2-4. ★★★ 求人の新着情報の自動配信（第274便）

- ★ 判定は**フクエス側とまったく同じ** `src/lib/announceAuto.ts`（純粋関数）。★ 2つの判定を持たない。
- ★ 印（`work_news.auto_rotate`）は既定 false。★ **店舗が印を付けるまで、周が回っても0件**。
- ★ 出す＝`published_at` を進める。★ 順番は `created_at` 昇順 → `id` 昇順で固定。
- ★★ **出せたときだけ** `rotation_index` を進める（★ 出す前に進めると1本飛ぶ）。
- ★★ 出さない店: 店舗が非表示／**求人が非公開**。★ 出し先が求人ページなので、見えないのに順番だけ進むのを防ぐ。
- ★ 進み具合は `salon_work_news_state`。★ オーナー様は**読めるが書けない**（1日1回の守りをすり抜けさせない）。

### 2-5. ★ TOPの店舗新着情報（第275便）

- ★ TOPは**1店舗1件**に間引く（`pickOnePerSalon`）。★ 自動配信で同じ店が毎日押し上がるため、
  間引かないと構造的にTOPが1店で埋まる。
- ★ 一覧 `/jobs/news` は**間引かない・最新50件**。★ 51件目は出さない。★ 履歴を見に来る場所だから。
- ★ 飛び先はどの行も**その店の求人詳細**（`/jobs/<求人ID>`）。★ 新着1本ごとのページは作らない。
- ★ `/jobs/news` は静的な区切りなので `/jobs/[id]` より先に一致する。★ 店舗ごとの `/jobs/<id>/news/<n>` とは別物。

---

## 3. ★★★ 実弾（2026-09-11）

| 時刻 | したこと | 結果 |
|---|---|---|
| 21:2x | `20260911_work_news_auto.sql` を Supabase で実行 | ★ Success（列と表ができただけ・挙動は不変） |
| 21:35 | 試し打ち（apply なし） | ★ `salons:1, posted:["6#0"], times:[{6,"15:42"}]` |
| 21:36 | 実弾（`apply:true`） | ★★★ `posted:1` ／ `/jobs/4` の新着が先頭・**NEW!!**・日付が9/11に |
| 21:37 | もう一度 apply なし | ★★ `posted:0, skipped:[{6,"done_today"}]` ＝**1日1回の守りが効いている** |
| 21:50 | `/jobs` のTOPブロック | ★ 「店舗新着情報」に1件表示・もっと見るも動作 |

### 3-1. ★★★ push の事故（今日2回目の形）

★ 第274便で `git add` が**1つも通っていなかった**。★ 原因は指示書のパス `src/app/jobs/\[id\]/...`。
　★ **PowerShell ではバックスラッシュがそのまま文字**になり、存在しないパス → git が止まって何も add しない。
　★ そのあと commit は「no changes added」、push は「Everything up-to-date」。★ 第271便と同じ見え方。
→ ★★ **指示書のパスは PowerShell 用に `'...'` で囲む**。★ `git add` のあとは必ず `git status --short` で
　`A` / `M` を目で見る。★ `git log --oneline -2` まで見る、は今までどおり。

### 3-2. ★★ ビルドを1回壊した（JSXコメント）

★ 三項演算子の中（`{t.user_id ? ( ... )}` の直下）に `{/* コメント */}` を置いて構文エラー。
　★ あの位置は**式が1つだけ**置ける場所。★ コメントを置くなら `//` の行コメント。

---

## 4. ★ 作業環境（今日ぜんぶ影響した）

- ★★★ **9/8のWindows更新**で、Cowork のワークスペースから `mnt/este-portal` が**マウントできない**。
  → ★ 全部「ファイルを取り込む → 直す → 書き戻す」でやった。★ `grep` も `tsc` もこちらでは走らせられない。
  ★ **型チェックはカッキーさん側の `npx tsc --noEmit` が唯一の関門**。
- ★★ 書き戻しで**同じ一時ファイル名を使うと、古い内容が送られる**ことがあった（並び順が反映されなかった件）。
  → ★ 以降は毎回**別名**で送り、**サイズを見て着弾を確認**する。★ 見た目だけで判断しない。

---

## 5. ★ 状態

```
git              第275便まで push 済み（★ 第274便 = c5aebb29）
SQL              20260911_work_news_auto.sql 適用済み（work_news.auto_rotate / salon_work_news_state）
自動配信          salon 6 に印あり・1本だけ → ★ 毎日その1本が出る。★ 時刻 15:42
cron             ★★ work-news-auto は【未登録】。★ 下の1行を crontab に足すまで自動では回らない
未追跡            Claude outputs/（コミットしていない）
```

★ cron（VPS・10分ごと）:

```
*/10 * * * * set -a; . /root/import.env; /usr/bin/curl -s -X POST https://fukues.com/api/admin/work-news-auto -H "Authorization: Bearer $CRON_SECRET" -H "Content-Type: application/json" -d '{"apply":true}' >> /root/import.log 2>&1
```

---

## 6. ★ 残っている小さいもの

```
・work-news-auto を crontab に登録する（★ いちばん大事。登録するまで手打ちのまま）
・自動配信が1本だけの店は毎日同じ記事が出る → 店舗に2〜3本付けてもらう案内
・写メ日記の並びを、出勤・セラピストと同じ（あいうえお順→非公開）に揃えるか
・漢字の名前を読み順に並べたい → therapists に読みがなの列
・新着情報の画像は中央基準で上下が切れる → 顔が切れるようなら object-top に（1行）
・既に11件以上ストックのある店は、次の投稿で10件に削られる（★ 今は該当なしと確認済み）
```

---

## 7. ★★★★ 今日（2026-09-11 夜）のまとめ

```
第272便  マイページ: 今すぐカードを写メ日記と同じ形／並びをあいうえお順→非公開／セラピストPC2列
第273便  求人の削除を撤去・セラピスト削除は非公開→2段階／応募の赤丸／クーポン文言
第274便  求人の新着情報に自動配信（1日1回・順番に1本）／ストック20→10／新着画像の比率固定
第275便  フクエスワークTOPに店舗新着情報＋一覧 /jobs/news（最新50件）
```

★ カッキーさんの判断で「作ったものを外す」が2回あった（応募バッジの定期更新・ヘッダーのサイトを見る）。
　★ どちらも**外すのは数分**。★ 迷ったら作って見てから決める、で今日はうまく回っている。

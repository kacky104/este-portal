# 指示書 第271便デプロイ 2026-09-11 — 駅ちかも、登録のあと自動で名簿を読み直す

★ 第270便（エステ魂）と同じことを駅ちかに。★ 同じ「いません」が起きる（★ 登録の照合は名簿を読んでいるが、写しは保存していなかった）。
★★ 触ったのは `src/lib/relayFlow.ts` 1本と番人2本。★ 店舗様の画面・運営の口・エステ魂は触っていない。

---

## 0. ★★★★ この便で変わること

```
前  駅ちかへ登録（＋写真）が通る → 終わり → 一覧を開き直すと「いません」→ 店舗様が「名簿を読み直す」を押す
後  駅ちかへ登録（＋写真）が通る → ★★ そのまま名簿（/admin/girls/）を GET で読み直す → 写しを保存 → 終わり → 開き直すと「います」
```

★ 連携の記録に「**登録のあと、駅ちかの名簿を読み直しました（N人）**」が1行足される（★ 「ログイン」は2回書かない）。

---

## 1. ★ 触ったもの（3本）

| ファイル | 前 → 後（bytes） | 中身 |
|---|---|---|
| `src/lib/relayFlow.ts` | 199,830 → 203,133 | ★ `advanceFlow` の**出口**で「girl_create・照合済み・done」を拾って `read_girls` へ繋ぐ／★ `afterReadGirls` は印（`createRosterRefresh`）が付いていれば `kind:'roster'`・記録1行 |
| `scripts/girlcreateflow-selftest.js` | 35,982 → 40,034 | ★ 118 → **130項目**（登録④の期待値 done→next・読み直し a〜e・印付きは二重に読まない） |
| `scripts/photoflow-selftest.js` | 52,839 → 53,942 | ★ 206 → **209項目**（登録の流れなら写真の照合のあと読み直しへ／運営の口は done／外れたら止まる） |

★★ `src/app/lib/media/relayFlow.ts` は**触っていない**。★ `kind:'roster'` の写しの保存（`saveRoster`・第50便）は intent を見ないので、そのまま乗る。

---

## 2. ★★★ 決めごと（★ エステ魂との違い）

| | エステ魂（第270便） | 駅ちか（この便） |
|---|---|---|
| 繋ぐ場所 | ★ 終わり方が2つ（写真なし／写真あり）なので、各 return に書いた | ★★ 終わり方が**4つ**（写真なし／1枚／複数枚／埋まった枠を飛ばして終わり）。★ 各 return に足すと1つ漏れるので、**`advanceFlow` の出口で1回だけ**拾う |
| 拾う条件 | intent=cast_create | ★ `kind==='done'` かつ `intent==='girl_create'` かつ **`createStage==='verify'`**（★ 登録の照合を通ったあと）かつ まだ読み直していない |
| 読み直さないとき | 写真の照合で外れた（stop） | ★ 同じ。★ さらに「同じ名前がもう居た」の done は**1回目**（createStage 無し）なので拾わない（★ 登録していないから） |
| 読む画面 | /admin/schedule/list/（出勤の名簿） | /admin/girls/（女の子一覧・★ roster_read と同じ画面） |

---

## 3. ★ 通した点検（クラウド側）

| | 結果 |
|---|---|
| `check:girlcreateflow` | ★ 118 → **130項目すべて ok** |
| `check:photoflow` | ★ 206 → **209項目すべて ok** |
| 巻き添え | `esutamaflow`(166) / `esutamaphotoflow`(95) / `articleflow`(179) / `diaryflow`(60) / `esutamadiarywire`(28) … **全部 緑** |
| 変異試験 | ★ 出口の繋ぎを外す → girlcreateflow NG 2・photoflow NG 1／★ 名簿の段の印を見ない → NG 4 |
| 型 | ★ strict で触った分 0件（★ 本番は §4 の `tsc --noEmit`） |

---

## 4. ★ 型チェック・ビルド・コミット（PowerShell）

```powershell
cd C:\Users\joltc\Desktop\este-portal
npx tsc --noEmit
npm run check:girlcreateflow
npm run check:photoflow
npx next build
```

★ `check:girlcreateflow` **130**・`check:photoflow` **209**。

```powershell
git add src/lib/relayFlow.ts scripts/girlcreateflow-selftest.js scripts/photoflow-selftest.js
git add "指示書_第271便デプロイ_2026-09-11.md"
git status --short
git commit -m "第271便: 駅ちかも登録のあと自動で名簿を読み直す（advanceFlow の出口で1回だけ）"
git push
```

★ 緑は **4本**。★ Vercel Ready を待つ。

---

## 5. ★★★ 本番で確かめる（★ 店舗様の画面から1人・写真あり）

### 5-0. 下ごしらえ

★ フクエスで **テスト16**（写真あり・CUP まで）を作る。★ 駅ちかの向きは read のまま。

### 5-1. ★★★ 実弾

```
① ホームで駅ちかを「フクエスから駅ちかへ反映」に
② ★ 1〜2分待つ（★ 向き変更に伴うジョブが終わるのを待つ）
③ セラピスト一覧 → テスト16 の【駅ちかへ登録】→ 1枚（写真: 1枚送ります…）→「登録する」
④ 2〜3分待って「連携の記録」
```

★★★ 期待の並び（上から新しい順）:

```
登録のあと、駅ちかの名簿を読み直しました（N人）        ← ★★ この便で増える1行
駅ちかの画像の枠 1 に写真を1枚登録しました（★ 読み直して確かめました）
駅ちかへ写真を1枚アップロードしました
（画像の編集ページを読んだ …）
テスト16さんを駅ちかに登録しました（新人マークつき）
（女の子一覧を読んだ・在籍N-1人）
駅ちかにログインしました
```

```
⑤ ★★★ 「名簿を読み直す」を押さずに セラピスト一覧を開き直す → テスト16 の駅ちかの列が「います」・ボタンが消えている
```

### 5-2. ★★ 後始末（★★★ この順番。★ 向きを先に戻すと削除が止まる）

```
⑥ castId … 連携の記録 か therapist_media_ids（provider='ekichika', therapist_id=<テスト16>）
⑦ VPS（★ 入り直したら . /root/import.env）:
     CAST=<castId>
     curl -s -X POST https://fukues.com/api/admin/media-girl-delete --oauth2-bearer "$CRON_SECRET" -d "salonId=6&castId=$CAST&apply=true"
   → applied:true（★ 駅ちかは非表示ではなく削除・第228便。★ 結びつきも外れる・第239便）
⑧ ホームで駅ちかを read に戻す
⑨ フクエスのテスト16 を削除
```

---

## 6. ★ 戻し方

★ この便のコミット1本を `git revert`（★ 登録・写真は第250便のまま動く。★ 読み直しだけ無くなる）。

---

## 7. ★ 次

```
・2枚目以降（★ まず1枚の決め）
・esutama-photo-push（運営の口）の stop を「ログイン情報がおかしい」にしない揃え
・★ 今日の一連（第263〜271便）で本線は閉じた。★ 次は別のテーマでよい
```

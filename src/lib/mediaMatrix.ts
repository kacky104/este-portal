// 反映の早見表（第212便・2026-09-07・カッキーさん）。★ 純粋なデータ。通信もDBも触らない。
//
// ★★★ 「設定を変えると、何が・どこへ・どれくらいで反映されるか」を1枚で見せる。
//   ★ 値はすべて【実際の動き】から（cron の間隔・受け口の条件）。★ 願望や予定を書かない。
//   ★ 「フクエスから反映」は【自動にした場合】の値（★ 自動にする前は毎回承認・第208便）。
//   ★ 第215便（2026-09-08）: 見出しの下の但し書き（note）は消した（カッキーさん）。★ 「自動にした場合」は補足の1行目で言う。
//
// ★ 出どころ:
//   出勤（自動）      … media-auto-push  30分ごと（crontab 5,35）
//   写メ日記 駅ちか   … forwardDiary（メール・即時）
//   写メ日記 エステ魂 … diary-auto-push  5分ごと（了承ありの方のみ）
//   写メ日記 エステラブ … メール・即時（投稿先は手で登録・第84便）
//   即セラ エステ魂   … sokusera-push    5分ごと（「今すぐ」ON→即セラON）
//   即ヒメ 駅ちか     … sokuhime-push 5分ごと（第215便・「今すぐ」ON→即ヒメON／OFFで枠を外す）
//                       ★ 自動にした店だけ（sokuhime_auto）。★ 取り込みは別（ingest-list・第39便）
//   新着情報 駅ちか   … article-auto    店舗の設定の時刻（read でも write でも出る・none では止まる）
//   駅ちか→フクエス 出勤 … import_interval_min（店舗ごと・既定60分・cron は15分ごと）
//   駅ちか→フクエス 週間出勤 … import/targets mode=full（1日1回・03:20）
//   フクエス→各サイト 週間出勤 … 出勤の送信は常に7日ぶん（同じ周）
//   駅ちか→フクエス 写メ日記 … diary-import 15分ごと（ログイン情報が要る）
//   エスラン         … 準備中（accepting: false・notYetKind 'preparing'）

export const MATRIX_SITES = ['駅ちか', 'エステ魂', 'エステラブ', 'エスラン'] as const;
export const MATRIX_ROWS = ['出勤', '週間出勤（7日分）', '写メ日記', '即ヒメ／即セラ', '新着情報'] as const;

export type MatrixSection = {
  key: 'write' | 'read' | 'none';
  title: string;
  /** 行 → 4サイトぶんの短い言葉 */
  cells: Record<(typeof MATRIX_ROWS)[number], readonly [string, string, string, string]>;
  /** ★ 表のすぐ下に出す注（※ のマスの説明）。★ 無ければ出さない（第215便） */
  remark?: string;
  /**
   * ★ 列の見出し。★ 無ければ MATRIX_SITES（4サイト）。
   * ★ 「駅ちかから反映」は【行き先】がフクエスなので、1列目を「フクエス」と書く（第215便・カッキーさん）。
   */
  headers?: readonly [string, string, string, string];
};

export const NO = '✕';
/** ★ そのサイトにその機能そのものが無い（送れない、ではない）。★ ✕ と見分ける（カッキーさん・2026-09-07） */
export const NA = '―';
/** ★ 「フクエスからは送らないが、そのサイト自身に同じ機能がある」。★ 表のすぐ下の remark で説明する（第215便・2026-09-08） */
export const SEE = '※';

export const MEDIA_MATRIX: readonly MatrixSection[] = [
  {
    key: 'write',
    title: 'フクエスから反映',
    // ★ 第215便（2026-09-08・カッキーさん）: 「◯分ごと」→「◯分以内」。
    //   ★ 周は◯分に1回回るので、入れてから反映まで【最長】◯分＋処理の1〜2分。★ 店舗様が知りたいのは「いつまでに載るか」。
    //   ★ 「駅ちかから反映」の側は取り込みの間隔が店舗ごとなので「ごと」のまま。
    cells: {
      '出勤':          ['30分以内', '30分以内', NO, '準備中'],
      // ★ 出勤の送信は常に7日ぶん（workPlan WORK_DAYS・esutamaPlan days）。★ 当日と同じ周で送る
      '週間出勤（7日分）': ['30分以内', '30分以内', NO, '準備中'],
      '写メ日記':      ['即時', '数分後', '即時', NA],
      '即ヒメ／即セラ': ['5分以内', '5分以内', NO, NA],
      // ★ エステ魂の新着情報は ✕ ではなく ※（サイト自身に自動更新がある・remark で言う）
      '新着情報':      ['設定の時刻', SEE, NO, NA],
    },
    remark: '※ エステ魂はサイトに新着情報の自動更新機能あり。そちらでの直接設定をお願いします。',
  },
  {
    key: 'read',
    title: '駅ちかから反映',
    // ★ 1列目は「駅ちかに入れたものがフクエスへ」なので、見出しは行き先の「フクエス」（第215便）
    headers: ['フクエス', 'エステ魂', 'エステラブ', 'エスラン'],
    cells: {
      '出勤':          ['15〜60分ごと', NO, NO, NO],
      // ★ 週間の予定は1日1回の周（import/targets mode=full・03:20）で維持する
      '週間出勤（7日分）': ['1日1回', NO, NO, NO],
      // ★ 第215便: 取り込みは15分ごとに回る＝書いてから【最長】15分（diary-import）
      '写メ日記':      ['15分以内', NO, NO, NA],
      '即ヒメ／即セラ': ['出勤と一緒', NO, NO, NA],
      '新着情報':      ['設定の時刻', NO, NO, NA],
    },
  },
  {
    key: 'none',
    title: 'どのサイトにも反映しない',
    cells: {
      '出勤':          [NO, NO, NO, NO],
      '週間出勤（7日分）': [NO, NO, NO, NO],
      '写メ日記':      [NO, NO, NO, NA],
      '即ヒメ／即セラ': [NO, NO, NO, NA],
      '新着情報':      [NO, NO, NO, NA],
    },
  },
];

/** ★ 早見表の下に置く1行ずつの補足（短く） */
export const MATRIX_FOOTNOTES: readonly string[] = [
  '出勤は、最初の1回を「出勤を送る」で確かめて送ったあと、自動にできます',
  'エステ魂の写メ日記・即セラは、ご本人の了承がある方だけ',
  '新着情報は駅ちか専用。「駅ちかの新着情報」で回数と文章を決めます',
  '即ヒメは、ほかのツールで自動にしているとそちらが優先されます',
  '― はそのサイトにその機能が無いもの',
];


// ────────────────────────────────────────────────────────────
// ★★★ セラピストの登録と名簿（第297便・2026-09-12・カッキーさんの指示）
//
// ★★ なぜ上の早見表に行を足さないのか
//   上の3つの表は【設定（フクエスから反映／駅ちかから反映／どのサイトにも反映しない）を変えると
//   何がどこへ流れるか】の表で、どれも【自動で回るもの】が並んでいる。
//   ★ セラピストの登録と名簿は【人が押したときだけ】動く（周は無い）。
//   ★ 同じ表に混ぜると「◯分以内」の仲間に見えて、放っておいても送られると読める。
//   → ★ 別の表にして、1行目に「押したとき」と書く。
//
// ★ 出どころ（すべて実際の動きから）:
//   登録        … TherapistBoard「◯◯へ登録」→ 内容を確かめる →「登録する」
//                 ★ 送れるのは駅ちかとエステ魂だけ（mediaCredentials.THERAPIST_CREATE_PROVIDERS）
//                 ★ 「フクエスから反映」の枠だけ（guardTherapistCreate ④・isWriteDirection）
//   写真        … 登録と同じ流れで1枚（フクエスの1枚目・トップ画像になる。第267〜269便）
//   登録後の名簿 … relayFlow の createRosterRefresh（第270・271便）。★ 自動で読み直す
//   名簿の読み取り … startMediaRosterRead（intent: 'roster_read'）。★ 押したときだけ・数分
//                 ★ 「どのサイトにも反映しない」の枠は押せない（第198便）
//   エステラブ   … 接続できません（accepting:false / notYetKind:'blocked'）→ 送れない・読めない
//   エスラン     … セラピストを扱わない（can は 'work' だけ）→ ―

export const THERAPIST_MATRIX_ROWS = ['新しい方を登録', '登録と一緒に写真', '登録のあとの名簿', '名簿の読み取り'] as const;

export type TherapistMatrix = {
  title: string;
  cells: Record<(typeof THERAPIST_MATRIX_ROWS)[number], readonly [string, string, string, string]>;
  remark: string;
};

export const THERAPIST_MATRIX: TherapistMatrix = {
  title: 'セラピストの登録と名簿',
  cells: {
    // ★ 周が無い＝放っておいても動かない。★★ どのボタンかが分かる言葉にする（第297便・カッキーさんの問い）。
    //   ★ 「押したとき」だけでは、何を押すのか分からなかった。★ ボタンの名前は下の注で書く。
    '新しい方を登録':   ['登録を押す', '登録を押す', NO, NA],
    '登録と一緒に写真': ['1枚',       '1枚',       NO, NA],
    '登録のあとの名簿': ['自動で読む', '自動で読む', NA, NA],
    '名簿の読み取り':   ['読み直す',   '読み直す',   NO, NA],
  },
  // ★★ どのボタンかを、画面に出ている文字のまま書く（★ 言い換えない＝探せる）。
  remark: '※ どちらも「セラピスト一覧」の画面から。登録はその方の行の「駅ちかへ登録」→ 送る内容を確かめて「登録する」、名簿は上の「駅ちかの名簿を読み直す」です。登録できるのは「フクエスから反映」にしているサイトだけ。',
};

/** ★ セラピストの表の下に置く補足（短く） */
export const THERAPIST_FOOTNOTES: readonly string[] = [
  // ★★★ 第297便（カッキーさんの確認）: この表だけを見ると、エステ魂の「押したとき」が
  //   【取り込み】に読める。★ 名簿の読み取りは「向こうに誰が居るか」を確かめるためのもので、
  //   フクエスへ情報を取り込むこと（駅ちかから反映）とは別。★ 取り込みは駅ちかだけ（方針）。
  '名簿の読み取りは、向こうに誰が居るかを確かめるためのものです（フクエスへの取り込みは駅ちかだけ）',
  '名簿の読み取りは押したときだけ。自動では回りません（数分かかります）',
  '登録できるのは、向こうの名簿にまだ居ない方だけです',
  '「どのサイトにも反映しない」にしている枠は、名簿も読みに行きません',
];

'use client';

import { ConecfShell, ConecfComingSoon } from '../../ConecfShell';

// コネックエフ「女性をサイトへ登録」（第395便・1a：外枠だけ。中身は準備中）

export default function ConecfGirlsSyncPage() {
  return (
    <ConecfShell current="girlsSync" title="女性をサイトへ登録">
      {() => <ConecfComingSoon />}
    </ConecfShell>
  );
}

★★★ このメモは古い。`引き継ぎメモ_2026-08-29_第49〜52便_名簿と見張り.md` に差し替わっている。
★ 同じ日のうちに第51・52便と full の事故が入ったため、内容が足りない。読むならそちらを。

---

# 引き継ぎメモ 2026-08-29（第49便）— 名簿の突き合わせ／駅ちか管理画面の調査

## 0. ★★★ まず読む — いまの状態を1行で

**駅ちかへは、まだ1文字も送っていない。**（第47〜48便メモから変わっていない）
送る道具は3便ぶん揃っていて、3つとも一度も動いていない。★ 実弾の日に3つ同時に初検証になる。

```
第46便  承認して送る経路      … まだ何も送っていない
第47便  止まったときの見張り  … まだ一度も出ていない
第48便  1回目だけ承認→自動    … まだ一度も動いていない
第49便  名簿の突き合わせ      … ★ デプロイすれば【その日に目視できる】
```
★★ 第49便は**4つ目を積んでいない**。すでにDBにある材料を見せるだけで、1行も書かない。

---

## 1. 済んだこと（第49便・未デプロイ）

```
新規  src/lib/mediaRoster.ts       名簿の突き合わせ（純粋関数・now も引数）
新規  scripts/roster-selftest.js   点検43項目
変更  src/app/actions/mediaCredentials.ts   getMediaRoster（読み取りだけ）
変更  src/app/mypage/MediaLinkPanel.tsx     「名簿の突き合わせ」節
変更  package.json / tsconfig.relaycheck.json
```
★ マイグレーション無し。★ 順序依存なし。★ 既存の関数・画面の既存部分は書き換えていない。
★ 型検査0件／点検4本すべて通過／eslint error 0。★ **まだ commit も push もしていない。**

→ 詳細と目視項目は `指示書_第49便デプロイ_2026-08-29.md`。

## 1-b. ★ 出しているもの（駅ちか・枠ごと）

```
フクエス N名（公開中 M名）／ 駅ちか（枠n）と結びついている K名
① 駅ちかと結びついていない                 castId が無い
② 結びついているが、フクエスに出ていない     ★ 非公開のまま or 退店
③ 駅ちかにいて、フクエスにいない            ★ 根拠が無ければ「分かりません」
```

---

## 2. ★★★ 迷ったらここへ戻る（第49便の設計の芯）

```
未照合0件（揃っている）        …  一覧は空
記録が無い（何も言えない）      …  一覧は空   ← ★ 画面上まったく同じ見た目
```
★★★ だから**根拠そのものを返り値に持たせた**（`RosterEvidence` ＋ `onlyOnMediaKnown`）。

```
paused  書き込みの向き → 取り込みが止まっているのは設計どおり。★ 警告にしない
none    記録が1件も無い → ★「分かりません」。0名と書かない
error   直近が失敗     → 数字を信じない（一覧が短く返っただけで全員が消えて見える）
stale   36時間以上前   → 「その時点のもの」と添える
fresh   新しい         → ★ このときだけ何も言わない
```
★★ **paused を最初に判定する**のが要。後回しにすると、正しく動いている書き込みの店に
　「取り込みが古い」と**嘘の警告**が出る。

---

## 3. ★★ 触ると壊れる所

```
1. mediaRoster.ts の中で Date.now() を呼ばない（now は引数。点検で古い状態を作れなくなる）
2. judgeRosterEvidence の判定順を入れ替えない（paused → none → error → 鮮度）
3. onlyOnMediaKnown が false のときに「0名」と表示しない ★ この便の存在理由
4. 取り込みの設定行が無い枠に「0名」と出さない（「設定がまだありません」と出す）
5. castId は必ず loadCastIds を通す。旧 import_cast_id を自前で読み替えない
   ★ 「旧列は駅ちかの枠1のときだけ混ぜる」規則が2か所に分かれる
6. therapists.is_active=false は「非公開」と「退店」の両方。文言で片方だけ言わない
7. getMediaRoster は読み取り専用。★ ここに更新を足さない（名簿を直す操作は別便・§76の調査待ち）
```

---

## 4. ★ 動いていないもの・足していないもの（第47〜48便から引き継ぎ）

```
・link_mode='write_auto' の枠は0（作るには1回目の承認＝実弾が要る）
・VPS の crontab に media-auto-push を足していない ★ 実弾のあと
・画面には第46便の 19:41 の古い計画が残っている
  ★ 実弾の日は必ず「反映内容を確認」を押し直す
・★ 新規: ingest-list（毎周）は salon_import_runs を1行も書いていない
  ★ ただし穴ではない。MODE=full（1日1回03:05）で list_mode の店も個人ページ経路へ流れ、
    実行ログは毎日更新される（scripts/import.sh: use_list = MODE=="list" and t.listMode）。
    影響は鮮度だけ ＝ ③が最大24時間古い。名簿は15分で変わらないので当面これで足りる。
```

---

## 5. ★★★ この環境（Claude 側）からできないこと（変わらず）

```
1. npm run build … 端末VMは SWC が linux/x64 で読めない
2. git push       … 端末VMのシェルに GitHub 認証情報が無い
3. Supabase への接続 … egress が通らない → SQL はカッキーさんに実行してもらう
★ git status で .git/index.lock が残る（削除許可が要る。第49便では消した）
★ 型検査は npx tsc --noEmit -p tsconfig.relaycheck.json で一次確認できる
```
自己点検（端末VMで動く）:
```
npm run check:roster      43項目  ★ 新規
npm run check:workplan    43項目  ★ 前メモの「45項目」は誤り。数えたら43
npm run check:linkmode    30項目
npm run check:linkstall   23項目
```

---

## 6. ★★★ 実弾の段取り（変わらず・3つが同時に初検証になる）

```
0. 店舗が「読み取りから書き込みに変えたい」と言う ← 起点
1. 文面を出す（社内用Aのチェックリストを先に通す）
2. 手動で1回送る          ← 第46便の初検証
3. 送れたことを確認        ← 24時間放置しない（第47便の見張りが出ないこと）
4. 数回、手動で回す
5. そのあとで自動にする    ← 第48便の初検証。★ 1回目の自動は必ず立ち会う
6. 自動を使い始めたら、文面の本文3「毎回必要です」を直す
```
★ 第49便を先にデプロイしておくと、1の前に**名簿のズレを店舗と確認できる**（§8 の人数の話）。

---

## 7. ★★★ §76 の調査は【済んだ】（設計メモ 追記18）

駅ちかの管理画面を実機で見た（掲載A・ログインID 37168）。★ **1つも保存していない。**

```
/admin/girls/                一覧    ★ 一括削除のフォームがある
/admin/girls/create/         新規登録
/admin/girls/edit/<castId>   編集    ★ 実物は edit/5232208&gl=FPl6
/admin/girls/delete/<castId> 個別削除 ★ confirm ダイアログ付き（押すな）
```
★★★ **castId ＝ 管理画面の girl_id ＝ 公開ページの番号**が3か所一致で確定
　（さら 5232208 が 設計メモ§7・編集URL・出勤フォームの hidden で一致）。

分かった:
```
□ 削除の口がある  ★★★ 個別 GET と 一括 POST の2種類
   制約: ランキング画像に設定中の子は消せない（別の子を設定してから）
□ 登録フォームの項目（110部品・name/cup/age/tall/3サイズ/questions[1..10]/genre[N] ほか）
□ hidden は fuel_csrf_token / p_genre_max_num / girls_genre_max_num
□ 編集にだけ「新人／体験入店＋新人」がある。体験入店は月枠あり（今月3/10人）
```
分からなかった（★ 推測で埋めないこと）:
```
□ 必須項目 … required 属性が1つも無い＝サーバ側検証。★ 試さないと分からない
□ 公開状態 … 項目そのものが無い。★「非公開で始まる」はベンリーの挙動。駅ちかは不明
□ 画像の経路 … file 欄が無い。一覧の写真は href が空＝JSで開くピッカー。別経路
```

★★ **道具はもう在る。** 登録も削除も「フォームを読む → fuel_csrf_token を取る → POST」で、
　第46便の出勤書き込みとまったく同じ形。★ 新しい仕組みは要らない。足りないのは知識だけ。

## 7-b. ★★★ 次の便でやること（設計メモ §81・この順番を崩さない）

```
1. 一覧・編集ページのパーサ（★ 読むだけ・危険ゼロ）  ← ★ 次はここ
   → castId・名前・状態が毎周取れる ＝ 宿題⑫（③が最大24時間古い）が解ける
   → ★ 実弾とも独立して進められる
2. 画像の経路を調べる（残った1項目）
3. ★ 削除を先に作る（登録より安全。消す相手は店舗が選ぶ）
4. ★ 登録は最後（重複掲載を作る側）
```
★ 3→4 の順は §4「新人登録を先にやらない」の延長。
　削除は失敗しても**同じ相手をもう一度消すだけ**。登録は失敗すると**増えたものが残る**。

## 8. 残っている宿題（設計メモ 追記17 §75）

```
①  セラピスト登録の画面   ★「見る」側は第49便で入った。★ §76 の調査は済んだ（追記18）
②  1回目だけ承認、以降は自動  ★ 実装済み。動かすのは実弾のあと
③  実弾                    ★ 文面はできた
④  枠2の衝突設計
⑥  出勤が他媒体へ広がるかの確認。実弾の1回目で見る
⑦  shrink の見張りは before が3名未満の日に効かない。当面は許容
⑧  自動を使う店が出たら文面の本文3を直す ★ 先回りして直さない
⑨  自動のしきい値を実データで決め直す
⑩  VPS の crontab に media-auto-push を足す（実弾のあと）
⑪  今すぐの幽霊（最長20分）は許容と決めた。直さない
⑫  ingest-list に実行ログを書かせるか（追記17 §72）。実弾のあとに決める
⑬  名簿のズレを【直す】操作。★ 削除→登録の順（§81）。★ 削除の口は在ることを確認済み
⑭  ★ 新規: 一覧・編集ページのパーサ（読むだけ）← ★ 次の便はここ
⑮  ★ 新規: 画像の経路の調査（§76 で唯一残った項目）
⑯  ★ 新規: edit URL の &gl= トークンが必須かの確認
```

---

## 9. ★ この便で拾った教訓

```
1. ★ 「0件」と「分からない」が同じ見た目になる場所は、型で分ける
   → 出るたびに気をつけるのではなく、返り値に根拠を持たせて、忘れられなくする
2. ★ 判定の順番そのものが仕様になることがある
   → paused を先に見ないと、正しく動いている店に嘘の警告が出る
3. ★ 1つの列に2つの意味が入っていることがある（is_active＝非公開／退店）
   → 列名を見ても分からない。文言で片方だけ言うと読み違える
4. ★ 「書いていない」を見つけたら、まず本当に困るのかを確かめる（第47便の教訓1の実践）
   → ingest-list が実行ログを書いていないのは事実だが、import.sh を読むと穴ではなかった
5. ★ 引き継ぎメモの数字も、書いた時点の話でしかない
   → 「check:workplan 45項目」は数えたら43だった。転記した数字を検算する
6. ★ 順番を守ると、置いたが動かないものが積み上がる。★ 積み上がったら
   「その日に目視できる便」を1つ挟む。リスクを増やさずに、動いている証拠が1つ増える
7. ★★★ 「無い」と言う前に、機能の名前で src 全体を grep する
   → relayFlow.ts だけ見て「CSRF を扱っていない＝実弾は失敗する」と言いかけた。
     実際は ekichikaWorkParse.ts の buildPayload の1行目に在った。
   ★ ファイル名で当たりを付けると、置き場所を勘違いしたときに気づけない（追記18 §79）
8. ★ 取り返しの付きやすさで順番を決める
   → 削除は失敗しても同じ相手をもう一度消すだけ。登録は失敗すると増えたものが残る。
     ★ 「危険が小さい方」ではなく「戻しやすい方」を先に作る
```

---

## 10. 次に読むもの

```
指示書_第49便デプロイ_2026-08-29.md   … この便の全部・目視・確認SQL
指示書_第48便デプロイ_2026-08-29.md   … 自動反映・実弾の段取り
指示書_第47便デプロイ_2026-08-28.md   … 見張り
設計メモ_第3弾書き込み画面_2026-08-28.md
    追記14 §52-60  自動反映の設計
    追記15 §61-64  実装して分かったこと
    追記16 §65-69  今すぐの幽霊。§12-1 の訂正と「許容する」決定
    追記17 §70-76  名簿の突き合わせ／次の関門（駅ちかのフォーム調査）
    追記18 §77-81  ★★★ 駅ちか管理画面の実測。URL・項目・削除の口・次の順番
店舗へ伝える文面_駅ちか書き込み_2026-08-28.md
```

# 引き継ぎメモ 2026-09-11 夜 — 第271便（駅ちかも、登録のあと自動で名簿を読み直す）

★ 第270便（エステ魂）と同じことを駅ちかに。★ 今日の一連（第263〜271便）の**最後の便**。
★★★ **実弾 通った**（18:11・店舗様の画面から・テスト19）。★ 「名簿を読み直す」を押さずに「います」になった。

---

## 0. ★★★ いまどうなっているか（3行）

- ★★★ 駅ちかへの登録（＋写真）が通ると、`advanceFlow` の**出口**で `read_girls` を1回だけ挟み、写しを保存してから終わる。
- ★★ 連携の記録に「**登録のあと、駅ちかの名簿を読み直しました（N人）**」が1行足される（★ ログインは2回書かない）。
- ★★★★ **これで駅ちか・エステ魂とも「ボタン1つで 登録 → 結びつけ → 枠1に写真 → 名簿の読み直し」**。★ 本線は閉じた。

---

## 1. ★ 触ったもの（3本）

```
src/lib/relayFlow.ts               199,830 → 203,133  ★ advanceFlow を「出口」と「段の本体（advanceFlowStep）」に分け、出口で girl_create・照合済み・done を拾う
                                                        ★ afterReadGirls は印（createRosterRefresh）付きなら kind 'roster'・記録1行
scripts/girlcreateflow-selftest.js  118 → 130項目
scripts/photoflow-selftest.js       206 → 209項目
```

★ `src/app` は触っていない（★ `kind:'roster'` の写しの保存は第50便のまま・intent を見ない）。

---

## 2. ★★ 決めごと（★ エステ魂との違い）

- ★ エステ魂（第270便）は終わり方が2つなので各 return に書いた。★ 駅ちかは**4つ**（写真なし／1枚／複数枚／埋まった枠を飛ばして終わり）なので、**出口で1回だけ**拾う。★ 各 return に足すと1つ漏れる。
- ★ 拾う条件: `done` かつ `girl_create` かつ **`createStage==='verify'`**（照合済み）かつ まだ読み直していない。
  ★ 「同じ名前がもう居た」の done は1回目（createStage 無し）なので拾わない（★ 登録していない）。
- ★ stop には重ねない（★ 止まったものは止める。★ 番号は登録の段で書いてある）。

---

## 3. ★★★ 実弾（2026-09-11）

| 時刻 | したこと | 結果 |
|---|---|---|
| 17:5x | テスト16・17・18 を【駅ちかへ登録】 | ★ **第271便がまだ push されていなかった**（git add のあと commit を飛ばして push → Everything up-to-date）。★ 第270便のコードで走ったので読み直しの行は無し。★ 一覧は「いません」 |
| 18:08 | commit → push（07761dd6）→ Vercel Ready | |
| 18:11 | テスト19 を【駅ちかへ登録】 | ★★★ ログイン → 名簿39名 → 登録 → 枠を読んだ → 写真 → **登録のあと、駅ちかの名簿を読み直しました（40人）** |
| 18:11 | 一覧を開き直す（★ 読み直しボタンは押していない） | ★★★ テスト19「**います**」 |

### 3-1. ★★★ 「push できているか」は `git log` で見る（★ 今日2回目）

★ `git add` → `git push` と打って `Everything up-to-date` が出ても、**commit が無ければ何も上がっていない**。
　★ 第268便でも「番人の add 漏れ」があった。★ 指示書の §4 は add → status → commit → push → **`git log --oneline -2`** の並びで固定した。
　→ ★★ Vercel の Deployments に「第N便: …」が **Ready** で出るまで実弾を打たない。

### 3-2. ★ 後始末で踏んだこと

- ★ SQL の `<テスト18>` を穴埋めせずに打った → 42601。★ **名前で引く SQL**（`t.name in ('テスト16', …)`）にすれば番号を調べずに済む。
- ★ Supabase の SQL エディタは DELETE の件数を出さない（「Success. No rows returned」）。★ 消えたかは **SELECT で確かめる**。
- ★ 駅ちかの後始末は、店舗様と同じく**駅ちかの管理画面で直接削除**でよい（★ 設計メモ §4 A-注）。★ 結びつきの行だけ SQL で消す。

---

## 4. ★ 番人・型

```
check:girlcreateflow   118 → 130（登録④の期待値 done→next・読み直し a〜e・印付きは二重に読まない）
check:photoflow        206 → 209（登録の流れなら写真の照合のあと読み直しへ／運営の口は done／外れたら止まる）
変異試験                出口の繋ぎを外す → girlcreateflow NG 2・photoflow NG 1／名簿の段の印を見ない → NG 4
巻き添え                esutamaflow 166 / esutamaphotoflow 95 / articleflow 179 / diaryflow 60 / esutamadiarywire 28 … 全部 緑
```

---

## 5. ★ 状態

```
git              第271便 push 済み（07761dd6・★ このメモは未コミット。★ .gitignore に弾かれるので git add -f）
salon 6          駅ちか → read に戻す／エステ魂 none
テスト16〜19      駅ちか: 直接削除済み／therapist_media_ids: 4行 delete 済み（SELECT で 0 rows）／フクエス: 削除
```

---

## 6. ★ 残っている小さいもの

```
・駅ちかの記録「駅ちか（枠1）で処理を行いました」（read_photo_page / crop の既定文）→ 言葉を付ける（エステ魂は第270便で済み）
・.gitignore が「引き継ぎメモ…夜…」「指示書_第271便…」を弾く理由（git check-ignore -v で見る）
・2枚目以降（★ まず1枚の決め）／esutama-photo-push の stop の揃え
```

---

## 7. ★★★★ 今日（2026-09-11）のまとめ

```
第263〜265便  エステ魂へも店舗様の画面から（登録だけ）
第266便       テスト運用中の帯を消す
第267便       運営の口: エステ魂も登録のあと写真を1枚（withPhoto 明示）… 15:00 実弾
第268便       運営の口: withPhoto を既定に … 試し打ち4発
第269便       店舗様の画面: エステ魂も写真まで … 16:13 実弾
第270便       エステ魂: 登録のあと自動で名簿を読み直す … 17:13 実弾
第271便       駅ちか: 同じ … 18:11 実弾
```

★ 昼の問い「新しいセラピストを作ったら駅ちかとエステ魂に自動で？」への答え:
　**自動ではなく【登録】ボタン1つ。★ ただし両媒体とも 登録 → 写真 → 名簿の読み直し まで1発。**
